class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        current = 0
        maxx = nums[0]
        for num in nums:
            current = max(current + num , num ) 
            maxx = max(current , maxx)

        return maxx
